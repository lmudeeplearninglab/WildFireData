# Core (required)
numpy>=1.26,<3
pandas>=2.0,<4
requests>=2.31
matplotlib>=3.8,<4          # <3.11 API deprecations already handled

# Geospatial (optional: polygon subsetting, fire-mask polygons, SHP/KML/GeoJSON)
# Pin these together -- the geospatial stack breaks across minor versions more
# than most.
geopandas>=1.0,<2
shapely>=2.1,<3             # 2.1 needed for concave_hull; 2.0 falls back to convex
rasterio>=1.3,<2            # GeoTIFF output; without it, .npy + bounds.txt fallback

# Clustering (optional but strongly recommended: without it every detection
# becomes its own footprint and no cluster labels are produced)
scikit-learn>=1.3,<2

# Earth Engine (optional)
earthengine-api>=0.1.380

# Caching (optional: persists EE results across runs; the single biggest
# quality-of-life win during development)
joblib>=1.3

# Basemaps (optional)
contextily>=1.5

# HDF5 output (optional, for --save-hdf5)
tables>=3.9

# Development
pytest>=8.0
pyflakes>=3.0
