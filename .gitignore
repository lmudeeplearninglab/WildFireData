# Credentials — never commit
.env
**/credentials
*.pem
*.key

# Run transcripts. Redacted for credentials, but they record local paths,
# fire names and date ranges.
report.txt
report.prev.txt
build_report.txt
**/build_report.txt

# Pipeline outputs
data output/
ml_dataset/
datasets/
*.npz
*.h5
*.tif
*.geojson

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/

# OS
.DS_Store
Thumbs.db

# NOTE: .pre-commit-config.yaml and .secrets.baseline SHOULD be committed.
# The baseline stores hashes of reviewed findings, not secrets; ignoring it
# makes every contributor re-triage the same findings on every commit.
